import { CSSProperties } from "react"

export const container: CSSProperties = {
  display: "grid",
  gridTemplateColumns: "120px auto",
  alignItems: "center",
  justifyItems: "center",
  margin: "30px",
}
